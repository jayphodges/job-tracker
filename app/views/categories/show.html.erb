All Jobs for <strong><%= @category.title %></strong>

<% @category.jobs.each do |job| %>
  <ul><%= job.title %> at <%= job.company.name %></ul>
<% end %>
<%= link_to 'Editz', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
